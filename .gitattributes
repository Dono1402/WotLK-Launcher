*.sh text eol=lf
*.py text eol=lf
*.caddy text eol=lf

# Unified patches contain a single-space marker for blank context lines.
mod-atlas-chat/hermes/*.patch text eol=lf whitespace=-blank-at-eol

# Migration manifests hash these bytes; keep the same LF form on every platform.
source/WotLK.Launcher.Server/Database/Migrations/*.sql text eol=lf
